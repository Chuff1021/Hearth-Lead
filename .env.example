# Database - SQLite for local dev, switch to PostgreSQL for production
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/hearth_leads?schema=public"

# Admin auth (simple shared secret for MVP)
ADMIN_SECRET="change-me-in-production"

# Optional: email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=app-password

# Optional: Google Search Console verification
# GOOGLE_SITE_VERIFICATION=your-verification-code

# Site URL (for sitemap generation)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_PHONE="(417) 555-0199"
NEXT_PUBLIC_BUSINESS_NAME="Hearth & Home Fireplace Co."
